import * as React from 'react';
import { View, Text } from 'react-native';

function IdPassword(props) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>IdPassword</Text>
        </View>
    )
}
export default IdPassword;